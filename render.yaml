services:
  - type: web
    name: tracktag
    env: node
    buildCommand: |
      apt-get update
      apt-get install -y chromium
      ls -l /usr/bin/chromium
      npm install && npm run build
    startCommand: npm run start
